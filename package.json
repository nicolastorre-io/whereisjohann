{
  "name": "whereisjohann",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "yarn workspace frontend dev",
    "build": "yarn workspace shared build && yarn workspace frontend build",
    "track:aisstream": "yarn workspace backend track:aisstream",
    "track:myshiptracking": "yarn workspace backend track:myshiptracking"
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
